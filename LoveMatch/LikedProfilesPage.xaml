<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LoveMatch.LikedProfilesPage"
             Title="Gelikede Profielen">

    <ScrollView>
        <StackLayout Padding="10" Spacing="10">

            <Label Text="Profielen die je geliked hebt" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center"/>

            <CollectionView ItemsSource="{Binding Members}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Stroke="Gray" StrokeThickness="1" Padding="10" Margin="5">
                            <StackLayout Spacing="10" HorizontalOptions="Center">

                                <!-- Profielfoto rond -->
                                <Border Stroke="Gray" StrokeThickness="1" HeightRequest="150" WidthRequest="150">
                                    <Image Source="{Binding ProfilePictureUrl}" Aspect="AspectFill"/>
                                </Border>

                                <Label Text="{Binding Name}" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center"/>
                                <Label Text="{Binding Age, StringFormat='Leeftijd: {0}'}"/>
                                <Label Text="{Binding Gender, StringFormat='Gender: {0}'}"/>
                                <Label Text="{Binding Location, StringFormat='Location: {0}'}"/>
                                <Label Text="{Binding Bio}" FontAttributes="Italic"/>
                                <Label Text="{Binding Interests, StringFormat='Interesses: {0}'}"/>
                            </StackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </StackLayout>
    </ScrollView>
</ContentPage>